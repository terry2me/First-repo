# ================================================================
# BB Monitor — 의존성 패키지
# 설치: pip install -r requirements.txt
#
# 구성:
#   [웹 서버]    FastAPI + Uvicorn
#   [데이터]     yfinance (주가 조회) + requests (HTTP)
#   [스케줄러]   APScheduler (매일 04:00 KST 자동 업데이트)
#   [타임존]     tzdata (Windows 환경 필수, Linux/Mac 선택)
#   [유효성]     pydantic (FastAPI 요청 모델)
# ================================================================

# ── 웹 서버 ──────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── 데이터 수집 ──────────────────────────────────────────────────
yfinance>=0.2.50
requests>=2.31.0

# ── 스케줄러 ─────────────────────────────────────────────────────
APScheduler>=3.10.0

# ── 타임존 (Windows 필수 / Linux·Mac 선택) ───────────────────────
tzdata>=2024.1

# ── 유효성 검사 (FastAPI 내장 의존) ──────────────────────────────
pydantic>=2.0.0
